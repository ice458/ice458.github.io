# ice458の物置き — サイト管理マニュアル

## 0. 手っ取り早く

| やりたいこと | すること |
|---|---|
| 記事を足す・直す | **`サイト管理.bat` をダブルクリック** |
| 見た目を確かめる | 管理ツールの「プレビュー」ボタン（または `プレビュー.bat`） |
| 公開する | VS Code の「ソース管理」からコミット → 同期（後述） |

これだけ覚えれば普段の運用は回ります。以下は詳細です。

---

## 1. 何が変わったのか

### 以前

各ページが完全に独立した HTML ファイルで、ヘッダ・フッタ・アクセス解析タグ・メタタグを
20ページ分すべてが自前で持っていました。`project_manager.py` と `blog_manager.py` は、
**HTML を正規表現で読み書きして** 一覧ページ (`index.html` / `blog.html`) を組み立てていました。

そのため、同じ情報が「一覧ページ」と「記事ページ」の 2 か所に重複して存在し、
両者を同期させるための機能（リンク検証・カテゴリ一括更新）が必要でした。

### 現在

各ページは**先頭の設定ブロック（front matter）＋ 本文**だけになりました。
共通部分は `_layouts/` と `_includes/` に 1 つずつ置いてあります。

**一覧ページは、各記事の front matter から自動生成されます。**
情報の置き場所が 1 か所になったので、同期という作業自体が消えました。

```
記事ページの front matter  ──┬──> 記事ページの <title> / メタタグ / 見出し
                             ├──> トップページの一覧の行
                             ├──> カテゴリ絞り込みボタン
                             └──> sitemap.xml
```

### 機能の対応表

| 以前のボタン | 今 |
|---|---|
| 新規記事追加 → 記事ページ生成 | **新規作成**（1 ステップに統合。フォルダと `img/` も同時に作成） |
| 編集 | **編集** |
| 削除 | **削除**（フォルダごと。確認あり） |
| 上に移動 / 下に移動 | **↑ 上に移動 / ↓ 下に移動** |
| 保存 | **保存** |
| リロード | **再読み込み** |
| プレビュー | **プレビュー**（本番と同じ URL 構造で表示されるようになりました） |
| リンク検証・修正 | **点検** に統合（＋画像の欠落や壊れたタグも検出） |
| 記事カテゴリ更新 | **不要になりました**（情報が 1 か所なので、ずれること自体が起きません） |
| （なし） | **本文を編集** — 本文を既定のエディタで開く |
| （なし） | **フォルダを開く** / **ブラウザで見る** |

---

## 2. 管理ツールの使い方

`サイト管理.bat` をダブルクリックすると起動します。
上部のタブで「プロジェクト」と「雑多なメモ等」を切り替えます。

### 新規作成

1. 「新規作成」を押す
2. フォームを埋めて OK
3. フォルダが自動で作られます（例 `project-1234567890/` と、その中の `img/`）
4. 「本文を編集しますか？」で **はい** を選ぶと、本文がエディタで開きます

**フォーム項目**

| 項目 | 必須 | 説明 |
|---|:--:|---|
| タイトル | ● | ページの見出し、`<title>`、一覧の行名になります |
| 日付 | | 「2026年08月05日」の形式。「今日」ボタンあり |
| 日付の見出し | | ページに「作成日: …」と出る部分の文言。既存ページに合わせて 3 種から選べます |
| 最終更新 | | ページ下部に出ます |
| カテゴリ | ● | 既存のものはチェックボックス。新しいものは下の欄に入力 |
| 一覧での説明 | ● | トップの一覧に出る短い文 |
| 検索用キーワード | | サイト内検索でヒットさせたい語。スペース区切り |
| 説明(meta) | | 検索エンジン向け。**空なら「一覧での説明」が自動で使われます** |
| キーワード(meta) | | 検索エンジン向け。カンマ区切り |
| YouTube動画 | | URL を貼るだけ。動画の構造化データが自動で付きます |
| 数式(MathJax) | | 数式を使うページだけチェック |

### 本文を書く

「本文を編集」を押すと `index.html` がエディタで開きます。
**先頭の `---` で囲まれた部分（front matter）は触らないでください。**
その下に本文の HTML を書きます。

よく使う書き方:

```html
<p>ふつうの段落。</p>

<div class="article-image">
    <img src="img/写真.webp" alt="説明">
    <p class="image-caption">キャプション</p>
</div>

<div class="article-section">
    <h2>見出し</h2>
    <p>中身。</p>
</div>
```

画像は、そのページのフォルダの中の `img/` に置いて `img/ファイル名` で参照します。
（「フォルダを開く」ボタンでエクスプローラが開きます）

### 並び替え

一覧での表示順は「↑ 上に移動 / ↓ 下に移動」で決めます。
日付順ではなく、**この順番がそのままトップページの並び**になります。

### 保存

編集した項目は、**「保存」を押すまでファイルに書き込まれません。**
未保存のものはタイトルの前に `*` が付きます。

### プレビュー

「プレビュー」を押すとサイトを組み立ててブラウザで開きます。
`http://127.0.0.1:4000/` で、本番とまったく同じ URL 構造で確認できます。

以前は `file://` で開いていたためリンクやパスが実際と違いましたが、
今は本番同様に確認できます。

### 点検

以下を自動でチェックします。

- URL（permalink）の重複や欠落
- 説明やカテゴリの空欄
- **本文から参照している画像が実在するか**
- **本文の `<div>` と `</div>` の数が合っているか**

---

## 3. 公開する（GitHub に反映する）

管理ツールは**ローカルのファイルを書き換えるだけ**です。公開には別途アップロードが要ります。

**VS Code を使う方法（コマンド不要）**

1. 左端の「ソース管理」アイコン（枝分かれのマーク）を開く
2. 変更されたファイルの一覧が出る
3. 上のメッセージ欄に何をしたか書く（例: 「〇〇の記事を追加」）
4. **「コミット」** を押す
5. **「変更の同期」**（または「変更のプッシュ」）を押す

push すると GitHub Actions が自動でビルドし、1〜2 分でサイトに反映されます。
反映状況は https://github.com/ice458/ice458.github.io/actions で確認できます。

---

## 4. ファイル構成

```
ice458.github.io/
├── サイト管理.bat            ← ダブルクリックで管理ツール起動
├── プレビュー.bat            ← ダブルクリックでプレビューだけ
├── site_manager.py           ← 管理ツール本体
│
├── _layouts/                 ← ページの骨格（共通）
│   ├── base.html             ...  <html> 全体、ヘッダ・フッタ・解析タグ
│   ├── page.html             ...  プロジェクト／メモの記事ページ
│   └── doc.html              ...  取扱説明書ページ
├── _includes/                ← 部品（共通）
│   ├── head.html             ...  <title> やメタタグ一式
│   ├── gtm-head.html         ...  アクセス解析・同意管理
│   ├── site-header.html      ...  上部のロゴとナビ
│   └── breadcrumb.html       ...  パンくずリスト
├── _tools/                   ← ローカルプレビュー用
│   ├── build.py
│   └── liquid_lite.py
│
├── index.html                ← トップ（一覧は自動生成）
├── blog.html                 ← メモ一覧（同上）
├── project-XXXXX/index.html  ← 各プロジェクト
├── blog/article-XXXXX/…      ← 各メモ
├── templates/                ← 新規作成時の本文のひな形
└── _site/                    ← プレビュー用の出力（git 管理外・消してよい）
```

**共通部分を直したいとき**は `_layouts/` か `_includes/` の中の 1 ファイルを直せば、
全ページに反映されます。以前のように 20 ファイルを触る必要はありません。

---

## 5. front matter の項目

ファイルを直接編集する場合の参考です。管理ツールを使うなら意識不要です。

```yaml
---
layout: page              # page（記事）/ doc（取説）/ base（一覧など）
type: project             # project / article / doc
nav: projects             # projects / blog … 上部ナビのどれを光らせるか
permalink: /project-16388/  # URL。★変更するとリンクが切れるので触らない
title: 8桁7セグLED駆動基板
description: 検索エンジン向けの説明
summary: 一覧に出す短い説明（description と同じなら省略可）
keywords: 表示器,電子工作,ice458
image: /logo.png          # OGP 画像
categories:
- 表示器
- 電子工作
date: 2023年09月23日
date_label: 作成日        # 「作成日」のときは省略可
updated: 2023年09月23日
search: 検索用の語
order: 5                  # 一覧での並び順（0 が先頭）
video: 2Aqbx-WZ33o        # YouTube の動画 ID
video_upload_date: 2023-09-23T00:00:00+09:00
mathjax: true             # 数式を使うときだけ
sitemap:
  priority: 0.7
  changefreq: monthly
---
```

---

## 6. 困ったとき

**Q. `サイト管理.bat` が一瞬で閉じる／英語のメッセージが出る**
Python が入っていないか、PATH に登録されていません。
https://www.python.org/downloads/ から入れ直し、インストール画面の
「Add Python to PATH」に必ずチェックを入れてください。

なお `.bat` の中身がすべて英語なのは意図的です。バッチファイルは
Windows のコマンドプロンプトが cp932 として読むため、日本語を書くと
文字化けするだけでなく、`if ( ... )` の対応が崩れてファイル全体が
壊れて動かなくなります。日本語の案内はすべてこのマニュアルと、
起動後の画面（PyYAML が足りないときの確認ダイアログなど）にあります。

**Q. プレビューが真っ白／崩れる**
管理ツールの「点検」を実行してください。本文のタグの閉じ忘れが原因のことが多いです。

**Q. 「ビルド失敗」と出た**
どのファイルのどこが悪いかがメッセージに出ます。
たいていは front matter の書式ミス（`:` の後のスペース抜け、インデントずれ）です。

**Q. プレビューでは出るのに本番に出ない**
push を忘れていないか、GitHub Actions が失敗していないかを確認してください。
https://github.com/ice458/ice458.github.io/actions

**Q. 間違って消してしまった**
git がすべて記録しています。VS Code のソース管理で変更を破棄するか、
履歴から復元できます。まだコミットしていなければ「変更の破棄」で戻ります。

**Q. プレビューと本番でズレることはある？**
`_tools/` のプレビューは、本番の Jekyll を Python で再現した簡易版です。
`_layouts` / `_includes` / front matter という同じ材料から組み立てるので
通常は一致しますが、100% 同一とは保証されません。
**最終確認は必ず push 後の本番ページで行ってください。**

---

## 7. 補足: 移行時に直したもの

今回の移行にあたって、以下は元の状態から意図的に変更しています。

1. **壊れていた HTML を修復** — 6 ページにタグの不整合がありました
   （ブラウザのエラー回復で表示できていた状態）。
   - `project-18775` / `22334` / `37877` / `38507` … `</div>` の閉じ忘れ
   - `blog.html` / `links.html` … `</div>` が 1 つ余分
2. **リンクをルート相対に統一** — パンくずなどの `../index.html` を `/` に変更。
   階層の深さに依存しなくなり、共通部品化が可能になりました。
3. **`privacy` / `links` / `tools` に OGP タグを追加** — 以前は付いていませんでした。
4. **重複していた解析タグの初期化を 1 つに整理** — 同じ関数を 2 回定義していました。

移行の前後で全 27 ページを機械的に比較し、**一覧の行データ（タイトル・リンク・
カテゴリ・説明・並び順）は完全一致**、その他の差分も上記の意図したものだけであることを
確認しています。URL は 1 つも変わっていません。
